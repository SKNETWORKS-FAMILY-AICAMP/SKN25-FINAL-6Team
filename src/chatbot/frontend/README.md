# Chatbot Frontend

챗봇 Streamlit 화면 코드를 두는 영역입니다.

현재 실행 wrapper는 `src/runners/streamlit_chatbot.py`이며, 실제 화면 구현은
`src/chatbot/frontend/app.py`에 둡니다.

나중에 프로젝트가 큰 `frontend/`와 `backend/` 구조로 분리되면 이 폴더의
화면 코드를 통합 프론트 영역으로 옮깁니다.
