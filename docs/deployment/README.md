# Deployment Docs

이 폴더는 환경별 실행과 배포 절차를 관리합니다.

현재 기준의 우선순위는 아래와 같습니다.

1. 외부 PostgreSQL + `psql` 로컬 검증
2. Docker Compose 기반 분리 실행
3. Supabase 전환

문서는 다음으로 나눕니다.

- `local-psql.md`
- `docker-compose.md`
- `supabase.md`
